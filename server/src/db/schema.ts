/**
 * The tables arrive in Task 3. `buildApp` needs the `Database` type to exist from Task 1,
 * because Kysely is a dependency of the app rather than of any one route.
 */
export interface Database {}
