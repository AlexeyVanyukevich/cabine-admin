/**
 * Placeholder until Task 2 brings up Postgres and a real booking engine here.
 * Vitest requires the file named in `globalSetup` to exist, and Task 1's tests
 * need neither container.
 */
export default async function setup(): Promise<void> {}
